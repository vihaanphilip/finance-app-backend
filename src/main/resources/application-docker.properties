# Docker Local PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5433/finance_db
spring.datasource.username=vihaan
spring.datasource.password=password123
spring.datasource.driver-class-name=org.postgresql.Driver

# Spring Data JDBC Configuration
spring.sql.init.mode=always

# Bootstrap data seeding for Docker profile
app.bootstrap-data=false
app.bootstrap-transaction-data=false
app.bootstrap-expense-data=false
app.bootstrap-earnings-data=false
app.bootstrap-test-runner=false

